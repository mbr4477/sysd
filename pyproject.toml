[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sysd"
version = "0.0.1"
requires-python = ">=3.8"
dependencies = ["fonttools", "Pillow", "typing_extensions", "svg.py"]
